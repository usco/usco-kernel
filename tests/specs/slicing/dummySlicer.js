var Kernel = require ("coscad-kernel");

Slicer = function() {
	//slice em up !

	Kernel.setSlicer( this );
};

Slicer.prototype={
	constructor: Slicer,
	
	sliceObject: function( geometry )
	{
		//do cool stuff
	}

	sliceProject: function( project )
	{
		//do cool stuff
	}
};
